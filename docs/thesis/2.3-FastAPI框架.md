2.3 FastAPI框架

FastAPI是一个基于Python的现代化Web框架，用于构建RESTful API接口。相比传统Python Web框架（如Flask、Django），FastAPI的核心优势在于：其一，原生支持异步（async/await），能够高效处理高并发请求；其二，通过Python类型注解自动完成请求参数的校验与文档生成；其三，自带交互式API文档（Swagger UI），访问`/docs`即可在线调试接口。