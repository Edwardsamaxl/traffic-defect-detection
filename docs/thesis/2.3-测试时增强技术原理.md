2.4 测试时增强技术原理

在模型推理环节，测试时增强（Test-Time Augmentation，TTA）通过构造输入图像的多个变体来优化最终预测[19]。具体而言，单张待测图像会经过若干几何变换得到不同视角的样本副本，这些副本并行送入已训练完毕的固定权重网络进行前向传播，随后将所有副本的预测输出经特定策略（如置信度加权或改进型NMS）聚合为唯一结果。由于整个过程中网络参数不发生任何更新，TTA属于零训练成本的轻量级后处理手段，能够在可控的算力开销内有效抑制因拍摄角度、尺度差异带来的检测波动，增强模型对边界场景的适应能力[20]。
