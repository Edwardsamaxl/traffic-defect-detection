2.4 FastAPI框架

本系统的后端推理服务选用FastAPI作为Web层框架。该框架基于Starlette异步引擎构建，天然兼容Python的async/await语法，可在单进程内通过事件循环调度大量并发连接，降低I/O等待对吞吐量的拖累。FastAPI借助Pydantic模型将请求体、路径参数与查询参数的类型声明直接转化为运行时校验规则，开发者无需额外编写校验代码即可在接口入口处拦截非法输入。同时，类型信息会被自动采集并渲染为符合OpenAPI规范的文档，框架内置的Swagger UI界面挂载于`/docs`路由，支持在线填充参数并即时发送请求，显著缩短了前后端联调周期。
