2.4 测试时增强技术原理

测试时增强（Test-Time Augmentation，TTA）是一种在推理阶段对输入图像进行多版本增强后集成预测的技术[19]。与训练时数据增强不同，TTA在保持模型参数不变的前提下，通过对测试图像施加几何变换（如翻转、旋转、缩放等）生成多个增强样本，分别推理后对检测结果进行加权融合或NMS后处理，从而提升检测结果的稳定性和鲁棒性[20]。TTA无需重新训练模型，计算开销适中，是一种实用的模型性能提升手段。
