2.6 MySQL数据库

MySQL是一种关系型数据库管理系统（RDBMS），用于存储结构化数据，支持SQL查询、事务管理和多用户并发访问。MySQL Server运行在指定主机上监听3306端口，接收并执行客户端发送的SQL语句，返回查询结果。

PyMySQL是MySQL的Python客户端驱动，负责将Python代码中的数据库操作请求转换为MySQL协议并发送至服务器，同时将执行结果转换回Python数据类型。开发者能够通过Python对象和类方法操作数据库，无需手写原始SQL语句，降低了数据库操作的学习成本和出错概率。
