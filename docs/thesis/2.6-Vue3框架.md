2.6 Vue3框架

前端可视化模块基于Vue3构建。该框架的核心运行时引入了Proxy驱动的响应式系统，对任意层级嵌套的数据对象建立细粒度依赖追踪；一旦观测数据发生变更，仅重新渲染关联的DOM子树，避免了命令式操作中全量重绘带来的性能损耗。在工程组织层面，Vue3推崇单文件组件（SFC）范式：每个`.vue`文件内部聚合了结构模板、交互逻辑与作用域样式三大区块，使同一功能域的代码高内聚地集中管理。组件之间通过Props与Emissions完成单向数据流通信，配合Composition API可将状态逻辑按功能维度抽离为可复用的组合函数，有效降低了复杂交互模块的维护难度。
