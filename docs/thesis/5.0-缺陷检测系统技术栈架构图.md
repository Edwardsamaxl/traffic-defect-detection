## 5.0 缺陷检测系统技术栈架构图

为说明系统实现阶段各技术组件之间的协作关系，本文将缺陷检测系统划分为前端层、后端服务层、算法推理层、数据存储层和运行环境层五个层级。各层并非简单的软件清单，而是围绕“用户发起检测请求、后端调度模型推理、结果持久化并返回前端展示”的主业务链路组织，如图5-X所示。

**图5-X 缺陷检测系统技术栈架构图**

```mermaid
flowchart TB
    %% ==================== 前端层 ====================
    subgraph L1["前端层"]
        direction LR
        UI["Web交互界面<br/>HTML / CSS / JavaScript"]
        VUE["Vue3<br/>页面组件与响应式状态"]
        ROUTER["Vue Router<br/>登录 / 检测 / 历史 / 仪表盘 / 模型管理"]
        AXIOS["Axios<br/>REST API请求与JWT请求头"]
        CHART["ECharts<br/>检测统计可视化"]

        UI --> VUE
        VUE --> ROUTER
        VUE --> AXIOS
        VUE --> CHART
    end

    %% ==================== 后端服务层 ====================
    subgraph L2["后端服务层"]
        direction TB
        APP["FastAPI应用<br/>Uvicorn ASGI运行"]
        SECURITY["安全与数据访问<br/>JWT认证 / bcrypt密码加密 / SQLAlchemy ORM"]

        subgraph API["API模块"]
            direction LR
            AUTH["Auth<br/>注册 / 登录 / Token校验"]
            DETECT["Detections<br/>单图检测 / 批量检测 / 结果封装"]
            DASH["Dashboard<br/>统计聚合 / 趋势查询"]
            MODELS["Models<br/>模型上传 / 列表 / 删除 / 选择"]
        end

        APP --> SECURITY
        APP --> AUTH
        APP --> DETECT
        APP --> DASH
        APP --> MODELS
    end

    %% ==================== 算法推理层 ====================
    subgraph L3["算法推理层"]
        direction TB
        CACHE["_MODEL_CACHE<br/>模型实例缓存"]
        TORCH["PyTorch<br/>张量计算与GPU加速"]
        YOLO["Ultralytics YOLOv8<br/>目标检测推理框架"]
        CBAM["CBAM改进模型<br/>注意力增强权重"]
        INFER["推理任务<br/>单图推理 / 批量推理 / TTA可选"]
        POST["后处理<br/>类别 / 置信度 / 边界框 / 标注图"]

        CACHE --> TORCH
        TORCH --> YOLO
        YOLO --> CBAM
        CBAM --> INFER
        INFER --> POST
    end

    %% ==================== 数据存储层 ====================
    subgraph L4["数据存储层"]
        direction LR
        MYSQL[("MySQL数据库<br/>users<br/>detection_records<br/>uploaded_models")]
        FS["文件系统<br/>上传模型<br/>批量检测输出<br/>标注结果"]
    end

    %% ==================== 运行环境层 ====================
    subgraph L5["运行环境层"]
        direction LR
        PY["Python 3.9+"]
        GPU["CUDA / GPU<br/>可选硬件加速"]
        OS["Windows / Linux<br/>本地部署环境"]
    end

    %% ==================== 主调用链路 ====================
    AXIOS -->|"1. 携带JWT提交检测请求<br/>图像 / 模型 / conf / iou / TTA参数"| APP
    APP -->|"2. 鉴权与参数校验"| SECURITY
    SECURITY -->|"3. 放行受保护接口"| DETECT
    DETECT -->|"4. 查询模型元数据"| MYSQL
    DETECT -->|"5. 读取上传图像或批量目录"| FS
    DETECT -->|"6. 调用推理服务"| CACHE
    MODELS -->|"模型上传与路径登记"| FS
    MODELS -->|"写入uploaded_models"| MYSQL
    POST -->|"7. 返回结构化检测结果"| DETECT
    DETECT -->|"8. 写入detection_records"| MYSQL
    DETECT -->|"9. 保存标注图与批量输出"| FS
    DETECT -->|"10. JSON结果 / 标注图路径 / 统计摘要"| AXIOS
    AXIOS -->|"11. 更新检测页结果与历史数据"| VUE

    %% ==================== 其它业务链路 ====================
    AUTH <-->|"用户注册登录<br/>bcrypt校验 / JWT签发"| MYSQL
    DASH -->|"读取detection_records生成统计"| MYSQL
    DASH -->|"统计数据"| CHART

    %% ==================== 运行依赖 ====================
    PY -.-> APP
    PY -.-> TORCH
    GPU -.-> TORCH
    OS -.-> APP
    OS -.-> FS
    OS -.-> MYSQL

    %% ==================== 样式 ====================
    classDef frontend fill:#E8F1FF,stroke:#2563EB,stroke-width:1.5px,color:#111827
    classDef backend fill:#EEF7ED,stroke:#16A34A,stroke-width:1.5px,color:#111827
    classDef algo fill:#FFF7E6,stroke:#F59E0B,stroke-width:1.5px,color:#111827
    classDef data fill:#F3E8FF,stroke:#7C3AED,stroke-width:1.5px,color:#111827
    classDef runtime fill:#F1F5F9,stroke:#475569,stroke-width:1.5px,color:#111827
    classDef focus fill:#FFE4E6,stroke:#E11D48,stroke-width:2px,color:#111827

    class UI,VUE,ROUTER,AXIOS,CHART frontend
    class APP,SECURITY,AUTH,DETECT,DASH,MODELS backend
    class CACHE,TORCH,YOLO,CBAM,INFER,POST algo
    class MYSQL,FS data
    class PY,GPU,OS runtime
    class DETECT,CACHE,POST focus
```

从主调用链路看，前端检测页通过 Axios 将用户上传的图像、模型选择和阈值参数提交至 FastAPI 后端。后端首先基于 JWT 完成身份校验，再由 Detections 模块查询模型元数据、读取输入文件并调用算法推理层。算法推理层通过 `_MODEL_CACHE` 复用已加载的 PyTorch/YOLOv8-CBAM 模型实例，完成单图或批量推理；若用户启用 TTA，则在推理阶段进行多尺度或翻转增强并融合预测结果。推理完成后，后处理模块生成缺陷类别、置信度、边界框坐标和标注图，后端将结构化结果写入 `detection_records` 表，将标注图或批量检测输出保存至文件系统，最后把 JSON 结果和资源路径返回前端展示。

